{% extends 'default.html.twig' %}

{% block body %}

<section>
	<div class="container">
        <div class="row paragraphe">

						<form action="/profil/newpass/{{ userid }}?identifier={{ link }}" method="POST">
						  <div class="form-row">
						    <div class="form-group">
						      						<label for="password_current">Current password</label>
						<input type="password" name="password_current" id="pasword_current"></br>

						    </div>
						    <div class="form-group">
						      <label for="password_new">New password</label>
								<input type="password" name="password_new" id="pasword_new"></br>
						    </div>
						  </div>
						  <div class="form-group">
						    <label for="password_new_again">New password again</label>
								<input type="password" name="password_new_again" id="pasword_new_again">
						  </div>
							<input type="hidden" name="token" value="{{ token }}">
						  <button type="submit" class="btn btn-primary">Update</button>
						</form>

						{% if error %}
                        <div class="mx-auto p-b-20">
                        {% for display in error %}
                            {{ display }}
                        {% endfor %}
                        </div>
                        {% endif %}

				</div>
		</div>
</section>

{% endblock %}
